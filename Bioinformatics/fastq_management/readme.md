## scripts to handle fastqs

### sample_to_size

bash scripts to samples all files in directory to a given size aproximatively. uses file size to sample proportion of reads. 

requires: 
	- seqtk
	- bgzip

###
